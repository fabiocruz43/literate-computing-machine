<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Minha Empresa - Sistema de Gerenciamento</title>
    <!-- Links para arquivos CSS se necessário -->
    <link rel="stylesheet" href="{{ url_for('static', filename='styles.css') }}">
</head>
<body>
    <h1>Bem-vindo ao Sistema de Gerenciamento da Minha Empresa</h1>

    <!-- Formulário para cadastrar cliente -->
    <form action="{{ url_for('cadastrar_cliente') }}" method="POST">
        <label for="nome">Nome do Cliente:</label>
        <input type="text" id="nome" name="nome" required>
        <button type="submit">Cadastrar Cliente</button>
    </form>

    <!-- Formulário para escolher pacote de serviços -->
    <form action="{{ url_for('escolher_pacote') }}" method="POST">
        <label for="pacote">Escolha um Pacote de Serviços:</label>
        <input type="text" id="pacote" name="pacote" required>
        <button type="submit">Escolher Pacote</button>
    </form>

    <!-- Formulário para escolher forma de pagamento -->
    <form action="{{ url_for('escolher_forma_pagamento') }}" method="POST">
        <label for="forma_pagamento">Escolha a Forma de Pagamento:</label>
        <select id="forma_pagamento" name="forma_pagamento" required>
            <option value="Cartão de Crédito">Cartão de Crédito</option>
            <option value="Boleto Bancário">Boleto Bancário</option>
            <option value="Transferência Bancária">Transferência Bancária</option>
        </select>
        <button type="submit">Escolher Forma de Pagamento</button>
    </form>

    <!-- Formulário para renovar plano -->
    <form action="{{ url_for('renovar_plano') }}" method="POST">
        <button type="submit">Renovar Plano</button>
    </form>

</body>
</html>
